use crate::proxy::send_messages;
use std::time::Duration;

pub fn start_timer() {
    // let local = state.lock().unwrap();
    // if local.timer_started {
    //     return;
    // }
    // let mut run_loop = stream::interval(Duration::from_secs(5));
    // while let Some(_) = run_loop.next().await {
    //     println!("prints every five seconds");
    // }
}
